---
# We have no artifactory EPEL mirror for Rocky so we'll just install the distribution's own
- name: Install EPEL
  ansible.builtin.dnf:
    name: epel-release
    enable_plugin: crb
    state: present
  become: true
